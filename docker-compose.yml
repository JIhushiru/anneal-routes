services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - osrm-cache:/app/.osrm_cache

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  osrm-cache:
